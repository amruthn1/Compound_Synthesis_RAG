# LLM Configuration
# Set to "true" to use OpenRouter API, "false" to use local model
USE_OPENROUTER=false

# OpenRouter API Configuration (only needed if USE_OPENROUTER=true)
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_api_key_here
